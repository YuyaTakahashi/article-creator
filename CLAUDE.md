# article-creator / article-post 作業ルール（厳守）

## WordPress への投稿は必ずローカルの Python で実行する

このリポジトリで WordPress（uxdaystokyo.com）へ投稿・更新するときは、**必ず**ユーザーのローカルターミナルで次のスクリプトを実行する。

```
cd ~/workspace/article-creator
python3 scripts/post_to_wp.py drafts/{ファイル名}.md
```

### 絶対にやってはいけないこと（再発防止）

- Cowork のサンドボックス（`mcp__workspace__bash`）から uxdaystokyo.com へ **一切アクセスしない**。POST だけでなく、GET・疎通確認・カテゴリ/タクソノミーの自動検出・メディアアップロードも全部ローカルの py に任せる。
- サンドボックスから外部 HTTPS は 403 でブロックされる。試すこと自体が無駄なので、接続テストすらしない。
- `web_fetch` で WP REST API を叩いて代用しようとしない。

### Claude がサンドボックスでやってよいこと

- MD の執筆・編集・ファクトチェック
- `drafts/` への MD 配置
- フロントマターの確認・修正（例: `category_field` は `glossary` 投稿タイプでは `glossary-category`）
- ローカル実行コマンドをユーザーに案内する

### 投稿フロー

1. Claude が `drafts/{ファイル名}.md` を整える（フロントマター含む）
2. Claude はローカル実行コマンドをユーザーに渡す（自分では POST しない）
3. ユーザーがローカルで `scripts/post_to_wp.py` を実行する
4. 成功時、スクリプトがフロントマターに `wp_post_id` を追記する（次回は更新モード）

## カテゴリのタクソノミー名

`glossary` 投稿タイプのカテゴリフィールドは `glossary-category` を使う（`categories` ではない）。

## ローカル実行の手段

WordPress への投稿は、サンドボックスではなく **Desktop Commander（`mcp__Desktop_Commander__start_process`）** で実行する。これはユーザーの Mac 上で直接動くため「ローカル実行」に当たり、サンドボックス禁止ルールに反しない。

```
cd ~/workspace/article-creator
python3 scripts/post_to_wp.py drafts/{ファイル名}.md
```

## 記事の書き方ルール（投稿前に必ず守る・再発防止）

過去に何度も外したので、MD を整える時点で必ず以下を満たす。

### 1. ルビ（外国人名・外国語の固有名詞）

人名などの外国語は、カタカナ＋括弧英語（例: `ロバート・M・ヤーキズ（Robert M. Yerkes）`）に **しない**。必ずルビ記法 `英字¥カタカナ¥` で書く。py が `<ruby>` に変換する。

- 正: `Robert¥ロバート¥ M.¥エム¥ Yerkes¥ヤーキズ¥`
- 誤: `ロバート・M・ヤーキズ（Robert M. Yerkes）`
- 英単語ごとに `¥カタカナ¥` を付ける。論文名・雑誌名・製品名（Duolingo 等）は対象外でそのまま英語表記にする。

### 2. タイトルに英語を入れない

`title` は日本語のみにする。「日本語 + 英語名」の併記は **しない**。

- 正: `title: "ヤーキズ＝ドッドソンの法則"`
- 誤: `title: "ヤーキズ＝ドッドソンの法則 Yerkes-Dodson Law"`

### 3. パーマリンク（slug）に日本語を入れない

フロントマターに英語（ローマ字）の `slug` を **必ず** 付ける。付けないと WordPress が日本語タイトルから日本語パーマリンクを自動生成してしまう。

- 例: `slug: "yerkes-dodson-law"`
- `scripts/post_to_wp.py` は `slug` があれば payload に載せて送る。

### 4. 最小限の説明 / excerpt は用語の性質に合わせて締める

`## 最小限の説明`（＝ `excerpt` と完全一致）は用語の定義文なので、解説対象の品詞に合わせて言い切る。動詞で締めると定義として不自然になる。

- 用語が「法則」なら「〜という法則」、傾向なら「〜する傾向」、現象なら「〜という現象」のように **名詞で締める**。
- 正（法則）: `適度な緊張で能力が最も高まり、強すぎても弱すぎても下がるという法則`
- 誤（動詞締め）: `人の力は適度な緊張で最も高まり、強すぎても弱すぎても下がる`
- これは本文の「体言止め禁止」ルールの **例外**。最小限の説明だけは、用語そのものを定義する一句なので名詞で言い切ってよい。20〜35文字・句点なしは従来どおり守る。

### 5. 提唱者の顔写真を毎回必ず入れる

毎回指示されなくても、投稿時に提唱者のポートレートを入れる。`## 語源・提唱者` の人物について Wikipedia の画像を取得し、**小さめ・中央寄せ**で、**キャプションに引用元URL** を付ける。肖像が存在しない人物（API が `NONE`）はスキップする。

画像URLは Wikipedia の pageimages API で取得する（サンドボックスで空が返るときは Desktop Commander でローカル実行）:

```
curl -s "https://en.wikipedia.org/w/api.php?action=query&titles={人物名}&prop=pageimages&piprop=original&format=json" | python3 -c "import sys,json; p=list(json.load(sys.stdin)['query']['pages'].values())[0]; print(p.get('original',{}).get('source','NONE'))"
```

挿入HTML（小さめ・中央・引用元URL入り）:

```html
<figure style="text-align:center;margin:1.5em auto;"><img src="{image_url}" alt="{name_katakana}" style="width:200px;max-width:45%;height:auto;display:block;margin:0 auto;"><figcaption style="font-size:0.85em;color:#666;">{name_katakana}（出典：{引用元URL}）</figcaption></figure>
```

### 6. 外部公開記事なので特定企業に寄せない（中立性）

UX TIMES は外部向けの一般記事である。本文・タイトル・`excerpt`（最小限の説明）・関連用語のどこにも、特定企業名（OPENLOGI 等）や、その企業固有の物流・EC 文脈への絞り込みを **入れない**。用語は中立的な一般解説として書く。

- ユーザー個人設定の「会社名は OPENLOGI 表記」ルールは社内文書向けであり、UX TIMES 記事には **適用しない**（例外として扱う）。
- 具体例を挙げるときも特定企業に寄せず、広く通用する例を選ぶ。

### 7. 定義文は挙動でなく「目的」で書く（冗長化の再発防止）

定義文が長くて何の話かわからなくなる原因は、画面上のふるまいを並べて説明していること。**その用語が何のために存在するのかを1文で言い切る**。挙動の説明は定義文の後ろの文に分ける。

- 型: `{用語}とは、{どんな働きによって}{何を実現する}{名詞}である`
- 誤（挙動の羅列・冗長）: `モーダル（Modal）とは、今の画面の上に重なって現れ、それに対応し終えるまで背後の操作をすべて止めてしまう画面要素である。`
- 正（目的で言い切る）: `モーダルとは、ユーザーの注意を強制的にひとつのことへ向けさせることで、操作を集中させるためのインターフェースである。`
- 動作を「〜し、〜する」と2つ以上並べたら冗長のサイン。「結局それは何のためか」を1つ立てて書き直す。
- **ユーザー視点で書き、システム側の仕様説明にしない。** 誤: `同じ操作がそのときのシステムの状態に左右されず、いつでも同じ結果になるように作られたインターフェースの性質` / 正: `システムの状態を意識しなくても、いつでも同じように操作できるインターフェース`
- 「〜してしまう」など書き手の評価・情緒を入れず、中立に機能を述べる。
- 英語名は本文初出で一度だけ示す。定義文の冒頭に併記しても（`モードレス（Modeless）とは、…`）、語源の文に回してもよい。禁止しているのはタイトルへの併記（ルール2）だけ。
- 適用範囲は `## 最小限の説明`（ルール4の名詞締めと併用）・導入リード文の1段落目・定義セクションの冒頭文。

### 8. 1段落に論点をひとつだけ置く

定義・具体例・語源・対概念を1段落に詰め込まない。論点が変わったら改段落する。1段落は2〜4文が目安。

- 各セクションの冒頭は、そのセクションの結論を **1文だけの独立した段落** にする。詳細は次の段落から書く。
- 具体例（たとえば〜）は定義の直後に置く。語源や周辺説明より先に出す。
- 1文に述語を3つ以上並べない。誤: `ナンバープレートを「NOMODES」にし、個人サイトの名前も nomodes.com にしたほどである` / 正: `自動車のナンバープレートや個人サイトにも「NOMODES」の名前を使っていたことでも知られる`
- 枝葉の固有名詞は落とす。誤: `ゼロックスのパロアルト研究所（PARC）で、同僚のTim¥ティム¥ Mott¥モット¥とワープロ「Gypsy」を作るなかで` / 正: `ゼロックスPARCでワープロソフト「Gypsy」を開発するなかで`

### 9. 語源・提唱者は「当時の課題 → 狙い → 何が変わったか」で書く

年号と人名の羅列で終わらせない。読者が「なぜこの概念が要るのか」を掴む手がかりは、提唱当時の状況にある。次の3点を必ず書く。

- **提唱当時、何が問題だったのか**：誰が・どんな現場で・何に行き詰まっていたのかを固有の状況として書く。「複雑さが増していた」「注目が高まっていた」のような一般論で済ませない。
- **その課題を解くために何を狙って作られたのか**：目的を「〜できるようにするため」の形でひとつ言い切る。「何をするものか」（手段）ではなく「何のためか」（狙い）を書く。
- **結果、何が変わったのか**：普及の経緯、後続の手法・概念への影響。
- 提唱当時の具体的なエピソード（実際のプロジェクト・製品・失敗事例）があれば必ず入れる。
  - 良い例（How-Might-We）: P&Gが競合の石鹸の模倣品を何種も試作して超えられずにいた、という具体状況から書き起こしている。
  - 悪い例（OOUX 初稿）: 「2013年ごろから提唱を始め、2015年に記事を発表した」という年表だけで、当時何に困っていたのかが書かれていない。

### 10. 効果・メリットは前後対比で書く

「一貫性が高まる」「効率化できる」といった抽象語の箇条書きで終わらせない。

- **使わない場合に実務で何が起きるか** → **使うとそれがどう変わるか** の順で対比を作る。この対比そのものが効果の説明になる。
- 抽象語を使うなら、それが実務で何を意味するのかを必ず後続の文で具体化する。リストの各項目を単語だけで終わらせない。
- **誰に効くのかを書き分ける**：エンドユーザー／作り手のチーム／事業。複数に効くなら分けて書く。
- 効果を裏づける数値・調査結果があれば併記する。裏づけのない効果は断定せず「〜とされる」と書く。

### 11. 記事後半に「実務でどう使われるか」を置く（4点は重要度順に、足りなければ落とす）

後半で読者がもっとも知りたいのは「自分の仕事でどう使うのか」である。中心となる活用法はひとつに絞り、次の4点を**重要度の高い順**に書く。

1. **どの工程で使うか**：企画・調査・設計・検証・運用のどの段階か
2. **何がアウトプットとして残るか**：図・一覧・問いの文・判断基準・合意事項など
3. **誰と使うか**：ひとりか、チームや他職種を巻き込むか。巻き込むならどの職種か
4. **実務で失敗しやすい点と、その回避策**

- **4点そろえることを目的にしない。** ソースに裏づけのない項目は、重要度の低いほうから落とす。推測で4点埋めるより、裏づけのある1〜2点を具体的に書く。
- **項目名をそのまま本文の語として使わない。** 「ありがちなつまずきは〜」と書き起こさず、その現場で何が起きるのかを直接書く。指示文の言い回しを本文へ写すと、どの用語の記事も同じ書き出しになる。
- 前セクションで述べた効能を言い換えるだけの記述にしない。読者が自分の現場に置き換えられる粒度まで具体的に書く。
- 正式な工程外での応用（会議・打ち合わせ・レビューでの使い方）があれば、末尾に足すか別セクションを立てる。
  - 良い例（How-Might-We）: 「実務での活かし方」で、議論が膠着した場面などに投げる問いを表で示している。
